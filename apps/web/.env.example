# Kopieren nach ".env" (nicht "env.example" umbenennen, die echte Datei muss ".env" heißen)
# und mit den Werten aus deinem Supabase-Projekt füllen: Project Settings -> API.
#
# WICHTIG: Hier gehört NUR der "anon public"-Key rein, NIEMALS der "service_role"-Key
# (der ist ausschließlich für die Edge Functions/Server bestimmt und darf nie in
# Client-Code oder ins Git-Repository gelangen).
VITE_SUPABASE_URL=https://tiwkhpuwyiramwjzaqee.supabase.co
VITE_SUPABASE_ANON_KEY=
